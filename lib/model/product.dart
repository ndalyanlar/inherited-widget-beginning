import 'package:flutter/material.dart';

class Product {
  final String? id;
  final String? pictureUrl;
  final String? title;
  final TextSpan? desc;

  Product({this.id, this.pictureUrl, this.title, this.desc});
}
